# backend/requirements.txt
fastapi
uvicorn[standard]
motor              # MongoDB async driver
pymongo
python-jose[cryptography]
passlib[bcrypt]
python-multipart
httpx
requests

# ── Docker SDK — para crear/destruir contenedores Liquidsoap dinámicamente ──
docker>=7.0.0

